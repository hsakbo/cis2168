public class hello{


//Full binary tree. A tree that has either 2 children nodes per parent or 2 leaf nodes.

//perfect binary tree. a tree that has all the leaf nodes for a level. (has to have 2^n -1).

//a complete binary tree. A perfect binary tree through n-1 with some extra leaf that has NO SINGLE LEAF WITHOUT SIBLING on the right.


    //list traversal: all recursive
    //inorder traversal-- left(first null) root right.
    //post order traversal -- left(first null) right root.
    //preorder traversal -- root(first null) left right.
    public static void main(String[] args){

	
    }

}
